# Optional SSH target used by `make doctor-gpu`.
# Examples:
# INFERNO_GPU_SSH=[operator supplied target]
INFERNO_GPU_SSH=

# Optional engine launch overrides. Defaults are pinned in configs/engines/vllm.json.
# INFERNO_ENGINE_HEALTH_TIMEOUT=900
