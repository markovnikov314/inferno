# Optional SSH target used by `make doctor-gpu`.
# Examples:
# INFERNO_GPU_SSH=user@gpu-host
# INFERNO_GPU_SSH="-i ~/.ssh/inferno_gpu user@gpu-host"
INFERNO_GPU_SSH=

# Optional P1 overrides. Defaults are pinned in configs/engines/vllm.json.
# INFERNO_ENGINE_HEALTH_TIMEOUT=900
